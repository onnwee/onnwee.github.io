# Backend Environment Configuration
# Copy this file to .env and update with your actual values
# NEVER commit your actual .env file to version control

# ========================
# DATABASE CONFIGURATION
# ========================
# PostgreSQL connection string (REQUIRED)
# Format: postgres://username:password@host:port/database?sslmode=disable
# For local development with docker-compose:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/onnwee_db?sslmode=disable

# ========================
# SERVER CONFIGURATION
# ========================
# Port the server listens on (OPTIONAL, defaults to 8080)
PORT=8080

# ========================
# OBSERVABILITY & TELEMETRY
# ========================
# Application environment for OpenTelemetry (OPTIONAL)
# Examples: development, staging, production
APP_ENV=development

# OpenTelemetry OTLP exporter endpoint (OPTIONAL)
# When set, traces will be exported to this OTLP collector endpoint
# Example: localhost:4318 for local collector (HTTP). Only HTTP is supported.
# Leave empty to disable trace export (noop mode)
OTEL_EXPORTER_OTLP_ENDPOINT=

# ========================
# DATABASE SEEDING
# ========================
# Number of fake users to generate when seeding (OPTIONAL, defaults to 500)
SEED_NUM_USERS=500

# Number of fake blog posts to generate when seeding (OPTIONAL, defaults to 500)
SEED_NUM_POSTS=500

# Number of fake projects to generate when seeding (OPTIONAL, defaults to 500)
SEED_NUM_PROJECTS=500

# Delay between seed operations to avoid overwhelming the database (OPTIONAL, defaults to 50ms)
# Format: duration string like "50ms", "100ms", "1s"
SEED_DELAY=50ms
